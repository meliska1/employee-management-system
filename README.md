 فتح المشروع (افتح مجلد المشروع باستخدام VS Code).
 تثبيت مكتبات Laravel (نفذ composer install).
 تثبيت مكتبات الواجهة (نفذ npm install).
 إنشاء ملف البيئة (انسخ .env.example إلى .env).
 إنشاء مفتاح التطبيق (نفذ php artisan key:generate).
 إعداد قاعدة البيانات (أنشئ قاعدة بيانات وعدّل ملف .env).
 إنشاء جداول قاعدة البيانات (نفذ php artisan migrate).
 بناء ملفات الواجهة (نفذ npm run build).
 تشغيل المشروع (نفذ php artisan serve).
 فتح المشروع (اذهب إلى http://127.0.0.1:8000 في المتصفح